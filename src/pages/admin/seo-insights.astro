---
// Moved to admin/analytics/seo.astro as part of the "Analytics
// Dashboards" nav reorg (see AdminLayout.astro and CLAUDE.md). This
// redirect keeps any old bookmark or hardcoded link to /admin/seo-
// insights working.
import { withBase } from '../../lib/url';

return Astro.redirect(withBase('/admin/analytics/seo'), 302);
---
