---
// Leads used to be one combined page (leads + Cloudflare traffic +
// Google Business Profile all stacked together) — split into its own
// "Leads" dashboard under the new "Analytics Dashboards" nav group (see
// AdminLayout.astro and CLAUDE.md's "Analytics Dashboards reorg"
// section). This redirect keeps any old bookmark or hardcoded link to
// /admin/leads working.
import { withBase } from '../../lib/url';

return Astro.redirect(withBase('/admin/analytics/leads'), 302);
---
